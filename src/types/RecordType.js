export type ScoreType = {
  home: string,
  away: string,
};
export type RecordType = {
  homeName: string,
  awayName: string,
  sport: string,
  start: string,
  state: string,
  id: number,
  score: ScoreType,
};
